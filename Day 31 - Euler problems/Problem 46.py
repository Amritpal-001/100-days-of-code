from EulerBase.PrimeNumbersFunctions import *

ListofPrimes = ListofPrimeNumbers(10000)
print(ListofPrimes)


def compoositeNumberList():
    list = []
    return(list)


def oddFilterforList(listName):
    OddfilteredList = []
    for number in listName:
        if number % 2 != 0:
            OddfilteredList.append[number]
    return(OddfilteredList)

def GoldbacConjectureApplicabeorNot(CompositeNumber):
    CompositeNumber = CompositeNumber
    if x = 2:
        Answer = True
    else:
        Answer = False
    returns(Answer , CompositeNumber  , Prime , )


GoldbacConjecture = True
while GoldbacConjecture == True:
    for compositeNumber in compoositeNumberList():
        x = GoldbacConjectureApplicabeorNot(CompositeNumber)
        GoldbacConjectureforThisNumber = x[0]
        if GoldbacConjectureforThisNumber == True:
            print(x)
        else:`
            print(compositeNumber)
            GoldbacConjecture = False

        L

